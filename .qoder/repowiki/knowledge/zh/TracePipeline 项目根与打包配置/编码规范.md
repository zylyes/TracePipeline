- 入口脚本统一采用 try/except KeyboardInterrupt 返回 130、except Exception 记录日志并输出中文错误提示再 sys.exit(1) 的错误处理模式。
- 启动时先调用 `trace_pipeline.config.ensure_workspace_dirs()` 初始化工作区目录，再进入主流程，确保 input/output 等目录存在。
- Windows GUI 启动前通过 ctypes 调用 user32 API 设置 Per-Monitor V2 DPI 感知，并在失败时回退到 SetProcessDPIAware。
- matplotlib 在所有 GUI 场景下通过 `force_noninteractive_backend()` 强制使用 Agg 后端，避免与 Tkinter/pywebview 事件循环冲突。