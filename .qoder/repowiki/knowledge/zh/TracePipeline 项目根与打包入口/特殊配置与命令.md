安装依赖：`pip install -r requirements.txt` 或 `pip install .[dev]`；运行 CLI：`python run_trace_pipeline.py --help` 或已安装的 `trace-pipeline --help`；启动 GUI：`python run_gui.py`；打包独立可执行：`pyinstaller TracePipeline.spec`，产物位于 `dist/TracePipeline/`。